# svg-route-finder
Web-based C++ pathfinding system that converts SVG files into geometry, builds a navigable graph, and finds optimal paths between user-defined points using A* and Dijkstra’s algorithms.
